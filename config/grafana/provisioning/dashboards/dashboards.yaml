apiVersion: 1

providers:
  - name: Mac LLM Cluster
    orgId: 1
    folder: Mac Cluster
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /opt/homebrew/etc/grafana/provisioning/dashboards
