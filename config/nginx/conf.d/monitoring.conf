# Grafana / Prometheus proxy (internal only)

server {
    listen 3001;
    server_name llm-01.local 192.168.10.11;

    location / {
        proxy_pass http://localhost:3001;
        proxy_set_header Host $host;
    }
}
